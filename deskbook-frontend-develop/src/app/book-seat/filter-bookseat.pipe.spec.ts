import { FilterBookseatPipe } from './filter-bookseat.pipe';

describe('FilterBookseatPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBookseatPipe();
    expect(pipe).toBeTruthy();
  });
});
