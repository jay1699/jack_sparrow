import { FilterSeatPipe } from './filter-seat.pipe';

describe('FilterSeatPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSeatPipe();
    expect(pipe).toBeTruthy();
  });
});
